jdk:
  - openjdk17
env:
  JITPACK: "true"
before_install:
  - git submodule update --init --recursive --depth 1
  - sudo apt-get update
  - sudo apt-get install -y swig cmake ninja-build
install:
  - ./gradlew :openvpn-library:assembleOvpn23Release :openvpn-library:publishToMavenLocal
